<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:color="@color/color_selected" android:state_checked="true" />  <!-- 선택된 상태 -->
    <item android:color="@color/color_unselected" /> <!-- 기본 상태 -->
    <item android:color="@color/color_text_selected" android:state_checked="true" />  <!-- 선택된 상태 -->
    <item android:color="@color/color_text_unselected" /> <!-- 기본 상태 -->
</selector>
